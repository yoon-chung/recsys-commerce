# .env.example -- copy to .env (gitignored) and fill in actual keys.
# Never commit .env to git -- only this template.

# Upstage Solar API (exp_008 LLM reranker)
UPSTAGE_API_KEY=up_your_solar_api_key_here

# Anthropic API (optional, future Claude reranker comparison)
# ANTHROPIC_API_KEY=sk-ant-your_key_here

# Weights & Biases (optional, already configured via wandb login if set)
# WANDB_API_KEY=your_wandb_key_here
