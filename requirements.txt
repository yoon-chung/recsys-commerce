# =============================================================
# Commerce RecSys - Local Development Requirements
# =============================================================
# 로컬PC는 코드 작성/디버깅용. 무거운 학습은 서버에서.
# 필요한 모델 라이브러리가 생기면 그때 추가.
# -------------------------------------------------------------

# ===== 데이터 처리 =====
pandas>=2.0
numpy>=1.24,<2.0
pyarrow>=14.0
scipy>=1.10

# ===== 추천 모델 (가벼운 것만) =====
implicit>=0.7         # ALS, BPR

# ===== 실험 관리 =====
wandb>=0.16
tqdm>=4.65
pyyaml>=6.0

# ===== EDA / 분석 (선택) =====
jupyter>=1.0
matplotlib>=3.7
seaborn>=0.13

# =============================================================
# 필요할 때 추가:
# - torch>=2.0           # 딥러닝 모델 (서버에선 이미 설치됨)
# - recbole>=1.2         # SASRec, BERT4Rec 등 시퀀셜
# - lightgbm>=4.0        # reranker
# - catboost>=1.2        # reranker
# =============================================================
